var elem = DOM.get(".minefield");
		
		var otherElem = DOM.div(".new", "Другой див"); //Создает новый Div в памяти (не на странице)
		
		elem.add(otherElem); //add добавляет элемент внутрь elem	
		elem.add(DOM.get(".zagolovok")); //add добавляет элемент внутрь elem
		
		elem.cls(".active"); //Добавить класс active
		elem.rcs("active"); //Удалить класс active у элемента
		
		elem.attr("url", "http://ya.ru"); //Добавить аттрибут url в elem
		var url = elem.attr("url"); //Взять значение аттрибута url
		
         	elem.html("Привет!");// Записать текст Привет внутрь
   		
		var newDiv = elem.div(".inner-element"); //Создать DIV с классом inner-element внутри elem
		var newDiv2 = elem.get(".inner-element");//Находит первый inner-element внутри
		var newDivs = elem.all(".inner-element");//Находит все inner-element внутри. Возвращает массив
		
		elem.del(".inner-element"); //Удалить элемент с классом inner-element внутри elem
   		elem.del(); //Удалить себя 